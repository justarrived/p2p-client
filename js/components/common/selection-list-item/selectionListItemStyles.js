import { StyleSheet } from 'react-native';

const SelectionListItemStyles = StyleSheet.create({
  listItem: {
    margin: 0,
  },
  listItemText: {
    marginLeft: 8,
  },
});

module.exports = SelectionListItemStyles;
