import { StyleSheet } from 'react-native';
import { WHITE } from '../../../resources/colors';

const JAListItemStyles = StyleSheet.create({
  listItemStyle: {
    paddingLeft: 17,
    marginLeft: 0,
    backgroundColor: WHITE,
  },
});

module.exports = JAListItemStyles;
