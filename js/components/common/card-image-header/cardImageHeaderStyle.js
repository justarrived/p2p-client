import { StyleSheet } from 'react-native';

const cardImageHeaderStyle = StyleSheet.create({

  coverPhoto: {
    flex: 1,
    width: null,
    height: null,
    justifyContent: 'center',
    padding: 10,
  },
});

module.exports = cardImageHeaderStyle;
